<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".tab4.descriptionActivity"></activity>
        <activity android:name=".tab4.addActivity" />
        <activity android:name=".tab3.descriptionActivity" />
        <activity android:name=".tab3.addActivity" />
        <activity android:name=".tab1.Description_item" />
        <activity android:name=".nav_calendar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tab1.addActivity" />
        <activity android:name=".tab2.addActivity" />
        <activity android:name=".tab2.descriptionActivity" />
    </application>

</manifest>